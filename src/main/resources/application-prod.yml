# ================= 生产环境配置 =================
server:
  port: 8080

spring:
  datasource:
    dynamic:
      primary: master
      strict: false
      datasource:
        master:
          url: jdbc:mysql://172.17.0.1:3306/positioning?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
          username: root
          password: f4sWWSJZpsBjkESA
          driver-class-name: com.mysql.cj.jdbc.Driver
        slave:
          url: ${SLAVE_DB_URL:jdbc:mysql://117.50.157.179:3306/elderly?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai}
          username: ${SLAVE_DB_USER:elderly}
          password: ${SLAVE_DB_PASSWORD:aT@87736339}
          driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: 172.17.0.1
      port: 6379
      password: 'nocache'
      database: 1
      timeout: 60s
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

  # 邮件配置（生产环境）
  mail:
    host: ${MAIL_HOST:webmail.csmsz.cn}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:sim4g@csmsz.cn}
    password: ${MAIL_PASSWORD:CH14725}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# MyBatis Plus 配置
mybatis-plus:
  configuration:
    # 生产环境关闭 SQL 日志输出
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
    # 驼峰命名自动映射
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      # 主键自增策略
      id-type: auto

# 物联云平台配置
iot:
  platform:
    app-id: ${IOT_APP_ID:zx20260707155447}
    app-key: ${IOT_APP_KEY:777fcc695baf9efec8b2b31288bb9be5}
    base-url: ${IOT_BASE_URL:http://zy.zyllz.com:8898/iot/api}

# 微信支付配置
wechat:
  pay:
    # 生产环境默认模拟模式，真实支付时改为 false 并填写下方配置
    mock: true
    app-id: ${WECHAT_APP_ID:wxc249d9bd4038838b}
    mch-id: ${WECHAT_MCH_ID:1748241775}
    mch-serial-no: ${WECHAT_MCH_SERIAL_NO:4CF49A6DE5E531AB08A0DAB9DB69D1A4ACBD1277}
    api-v3-key: ${WECHAT_API_V3_KEY:KiHxewhciQkn9sLYkV8pfxEDY4d5jPQa}
    private-key-path: ${WECHAT_PRIVATE_KEY_PATH:/opt/new-service/key/apiclient_key.pem}
    notify-url: ${WECHAT_NOTIFY_URL:http://47.94.183.144:8082/api/mobile/pay/callback}

logging:
  level:
    root: WARN
    com.zns.positioning: INFO