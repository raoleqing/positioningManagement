# ================= 公共基础配置 =================
spring:
  application:
    name: positioningManagement
  # 默认激活 dev 环境
  profiles:
    active: dev

server:
  servlet:
    context-path: /api

logging:
  file:
    path: logs/positioningManagement.log
